% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oracle_utils.R
\name{configure}
\alias{configure}
\title{Set the connection and credentials for this session}
\usage{
configure(con = NULL, jdbc_url = NULL, username = NULL, password = NULL)
}
\arguments{
\item{con}{A \code{spark_connection} (from \code{sparklyr::spark_connect()}) or a
\code{DBIConnection} (from \code{DBI::dbConnect()}).}

\item{jdbc_url}{Oracle JDBC connection string. Spark path only.}

\item{username}{Oracle account name. Spark path only.}

\item{password}{Oracle account password. Spark path only.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Arguments left \code{NULL} are not assigned, so an existing value is kept rather
than cleared -- call it repeatedly to update settings piecemeal.
}
\section{Which settings you need}{


It depends on what \code{con} is, and that is the whole reason this port takes a
connection object where the Python original took a \code{SparkSession}:

\describe{
\item{a \strong{sparklyr} connection}{\code{jdbc_url}, \code{username} and \code{password} are
also required. Spark opens the Oracle connection itself, per query, from
the JDBC options this package assembles.}
\item{a \strong{DBI} connection}{the connection \emph{is} the credential. Whatever
you passed to \code{DBI::dbConnect()} already carries the account, so the
three JDBC settings are ignored and need not be supplied.}
}
}

\examples{
\dontrun{
# DBI: the connection carries the credentials
configure(con = DBI::dbConnect(odbc::odbc(), dsn = "ORCL"))

# sparklyr: Spark needs the JDBC settings to hand to the driver
configure(
  con = sc,
  jdbc_url = "jdbc:oracle:thin:@//host:1521/service",
  username = "reporting",
  password = Sys.getenv("ORACLE_PASSWORD")
)
}
}
\seealso{
\code{\link[=is_configured]{is_configured()}}, \code{\link[=the_connection]{the_connection()}}, \code{\link[=reset_configuration]{reset_configuration()}}
}
