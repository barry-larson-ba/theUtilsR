% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosstab.R
\name{exclude_faccodes_expr}
\alias{exclude_faccodes_expr}
\alias{exclude_faccodes}
\title{Drop rows carrying an administrative (non-reportable) faccode}
\usage{
exclude_faccodes_expr(excluded = EXCLUDED_FACCODES)

exclude_faccodes(data, excluded = EXCLUDED_FACCODES)
}
\arguments{
\item{excluded}{Character vector of administrative codes.}

\item{data}{A data frame or lazy table.}
}
\value{
\code{exclude_faccodes_expr()}: a quosure, for \code{filter()}.
\code{exclude_faccodes()}: \code{data}, filtered.
}
\description{
The \code{is.na()} arm is not decoration. \code{!(faccode \%in\% excluded)} evaluates to
\code{NA} for a missing faccode, and both \code{dplyr::filter()} and a SQL \code{WHERE}
drop those rows -- whereas the pandas original keeps them, because
\code{~Series.isin()} returns \code{TRUE} for \code{NaN}. Naming the case explicitly keeps
the R port answering what the Python one answers.
}
\examples{
rows <- tibble::tibble(faccode = c("OAK", "REG", NA))
exclude_faccodes(rows)
}
