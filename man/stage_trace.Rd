% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{stage_trace}
\alias{stage_trace}
\title{The row counts recorded by the last \code{run_stages()}}
\usage{
stage_trace(x = NULL)
}
\arguments{
\item{x}{A frame returned by \code{\link[=run_stages]{run_stages()}}. Omit it to read the trace from
the most recent run in this session.}
}
\value{
A tibble of \code{stage}, \code{rows} and \code{delta}. \code{rows} is \code{NA} when counting
was skipped; see \code{\link[=run_stages]{run_stages()}}.
}
\description{
The row counts recorded by the last \code{run_stages()}
}
\section{Where the attribute goes}{


dplyr verbs preserve it, on local frames and lazy tables alike. \code{collect()}
does not -- and that is the one operation every remote report ends with, so
calling \code{stage_trace()} with no argument is the reliable form after a
collected run rather than a fallback for unusual cases.
}

\seealso{
\code{\link[=run_stages]{run_stages()}}
}
