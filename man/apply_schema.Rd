% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oracle_utils.R
\name{apply_schema}
\alias{apply_schema}
\title{Cast columns to the types named in a schema}
\usage{
apply_schema(data, schema, con = NULL)
}
\arguments{
\item{data}{A data frame or lazy table.}

\item{schema}{Named character vector of column -> canonical type.}

\item{con}{The connection, used to pick the SQL spelling. Only consulted for
lazy tables.}
}
\value{
\code{data}, with the casts applied.
}
\description{
This casts existing columns rather than replacing the frame's schema: every
name in \code{schema} must already be present as a column, and columns not named
keep whatever type they had.
}
\details{
\code{schema} is a named character vector of column name to canonical type, one
of \code{character}, \code{integer}, \code{bigint}, \code{double}, \code{logical}, \code{date},
\code{timestamp}. Canonical rather than backend-native so the same schema
definition survives a move from Oracle to Spark -- the only spelling that
actually differs is the string type, which Spark calls \code{STRING} and everyone
else calls \code{VARCHAR}.
}
\section{Why it checks first}{


A missing column is caught up front so the message names what is missing and
what is available. Letting the engine hit it instead produces an
\code{UNRESOLVED_COLUMN} buried in a Java stack trace, which hides the one useful
line.
}

\examples{
apply_schema(
  tibble::tibble(pract_id = "101", month = "0"),
  c(pract_id = "integer", month = "integer")
)
}
