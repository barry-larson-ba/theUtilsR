% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oracle_utils.R
\name{read_oracle}
\alias{read_oracle}
\title{Read an Oracle query}
\usage{
read_oracle(
  query,
  con = the_connection(),
  schema = NULL,
  auto_fix_decimals = TRUE,
  decimal_scales = DEFAULT_DECIMAL_SCALES,
  return_type = "source",
  types = NULL
)
}
\arguments{
\item{query}{An Oracle SELECT statement, without a trailing \verb{;}. See
\code{\link[=read_oracle_raw]{read_oracle_raw()}} for the syntax constraints.}

\item{con}{A connection. Defaults to the configured one.}

\item{schema}{Optional named character vector of per-column casts, applied
as described in \code{\link[=apply_schema]{apply_schema()}}.}

\item{auto_fix_decimals}{Whether to run \code{\link[=convert_integer_decimals]{convert_integer_decimals()}}.}

\item{decimal_scales}{Passed to \code{\link[=convert_integer_decimals]{convert_integer_decimals()}}; ignored unless
\code{auto_fix_decimals} is \code{TRUE}.}

\item{return_type}{One of \link{RETURN_TYPES}. \code{"source"} leaves the result on
the server; the others collect to driver memory.}

\item{types}{Optional named character vector applied after collection;
ignored when \code{return_type} is \code{"source"}.}
}
\value{
A lazy table, tibble, data frame or \code{data.table}.
}
\description{
The intended entry point. Stages run in this order:
}
\details{
\enumerate{
\item \code{\link[=read_oracle_raw]{read_oracle_raw()}} -- execute the query
\item \code{\link[=convert_integer_decimals]{convert_integer_decimals()}} -- scale-0 decimals to \code{BIGINT}
(a no-op off the Spark path)
\item \code{\link[=apply_schema]{apply_schema()}} -- explicit per-column casts
\item collection -- \code{source}, \code{tibble}, \code{data.frame} or \code{data.table}
}

Step 2 precedes step 3 so that a caller-supplied \code{schema} always wins over
the automatic decimal handling. Preserve that ordering when modifying this.
}
\section{Validation comes first}{


\code{return_type} is checked before the query is issued, so a typo raises
immediately rather than after a multi-minute Oracle round trip. Keep new
argument validation up there with it.
}

\examples{
\dontrun{
df <- read_oracle("SELECT * FROM claims.members")

ids <- read_oracle(
  "SELECT member_id FROM claims.members",
  return_type = "tibble",
  types = c(member_id = "bigint")
)
}
}
