% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_transforms.R
\name{first_per_group}
\alias{first_per_group}
\title{Keep the top row per group, ranked by an ordering column}
\usage{
first_per_group(data, group, order_by, desc = TRUE)
}
\arguments{
\item{data}{A data frame or lazy table.}

\item{group}{Character vector of grouping columns.}

\item{order_by}{Character vector of ordering columns, most significant
first.}

\item{desc}{Either a single \code{TRUE}/\code{FALSE}, or one per \code{order_by} column.}
}
\value{
\code{data}, reduced to one row per \code{group}, ungrouped.
}
\description{
\code{desc} accepts either one value for every ordering column or one per column,
so a ranking can mix directions -- "highest count, then earliest name" being
the common case for making a tie-break deterministic.
}
\details{
Missing values sort \strong{last} in every backend and in both directions. No
engine does that by default and no two engines agree, so this asks for it
explicitly; without it an \code{NA} in an ordering column would pick a different
winner depending on where the report ran.
}
\examples{
counts <- tibble::tibble(
  pract_id  = c(1, 1, 2),
  expertise = c("TeleICU", "TeleStroke", "TelePsych"),
  fac_count = c(2, 2, 1)
)
# highest count, ties broken on the earliest program name
first_per_group(counts, "pract_id", c("fac_count", "expertise"), c(TRUE, FALSE))
}
