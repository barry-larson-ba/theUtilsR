% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oracle_utils.R
\name{convert_integer_decimals}
\alias{convert_integer_decimals}
\title{Recast integer-valued decimal columns to a 64-bit integer type}
\usage{
convert_integer_decimals(data, scales = DEFAULT_DECIMAL_SCALES)
}
\arguments{
\item{data}{A lazy table.}

\item{scales}{Integer vector of decimal scales to convert.}
}
\value{
\code{data}, with qualifying columns recast; unchanged if none qualify.
}
\description{
Oracle numeric columns arrive over JDBC as decimals, which is faithful but
awkward downstream: arithmetic carries 38-digit decimals, joins against
genuine integer columns need casts, and collecting to R yields character or
list columns rather than numbers.
}
\details{
Which decimal you get depends on how the Oracle column was declared, and the
distinction matters:

\preformatted{
  NUMBER(10, 2)   -> DecimalType(10, 2)    a real fixed-point value
  NUMBER(38)      -> DecimalType(38, 0)    scale explicitly 0
  NUMBER          -> DecimalType(38, 10)   scale not declared at all
}

That last row is the one that surprises people. A bare \code{NUMBER} does not
report a scale, and Spark's Oracle dialect represents it as
\code{DecimalType(38, 10)} -- so an ID column declared as plain \code{NUMBER} shows up
with ten decimal places it never uses, and is \emph{not} touched by the default
\code{scales = 0}.

Pass \code{scales = c(0, 10)} to catch those as well. That is opt-in rather than
the default because scale 10 is ambiguous: it covers both integer IDs and
genuinely fractional values, and the cast truncates toward zero, silently.
Only widen the scales for columns you know hold whole numbers.
}
\section{This is a Spark-path concern only}{


On a DBI connection there is no Decimal intermediary -- the driver maps
Oracle \code{NUMBER} to an R numeric on the way out -- so there is nothing to
recast and this function returns \code{data} unchanged. It is still called
unconditionally by \code{\link[=read_oracle]{read_oracle()}} so the pipeline reads the same either
way.
}

