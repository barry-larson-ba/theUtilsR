% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_transforms.R
\name{facility_status_expr}
\alias{facility_status_expr}
\alias{classify_facility_status}
\title{Classify a row's facility status as P / C / T / blank}
\usage{
facility_status_expr()

classify_facility_status(data, into = STAT_COLUMN)
}
\arguments{
\item{data}{A data frame or lazy table.}

\item{into}{Name of the column to write the status into.}
}
\value{
\code{facility_status_expr()}: a quosure.
\code{classify_facility_status()}: \code{data} with the \code{into} column added.
}
\description{
\code{facility_status_expr()} returns the rule as a quosure; the expression form
matches the Polars and Spark halves of the Python original.
\code{classify_facility_status()} applies it.
}
\details{
Expects the columns \code{primary_fac_flag}, \code{current_status} and
\code{status_category}.
}
\section{On missing values}{


An \code{NA} in any tested column makes its condition \code{NA}, which \code{case_when()}
treats as "not matched" and falls through -- so a row with nothing known
about it lands on \code{STATUS_INACTIVE}. That is the same answer the pandas
original gives, where the comparisons coerce \code{NaN} to \code{FALSE}, and the same
answer SQL gives, where the \verb{CASE WHEN} branches see \code{NULL}.
}

\examples{
rows <- tibble::tibble(
  primary_fac_flag = c("Y", "N", "N", "N"),
  current_status   = c("CURRENT", "CURRENT", "CURRENT", "EXPIRED"),
  status_category  = c("ACTIVE", "COURTESY", "TELEMEDICINE AFFILIATE", "ACTIVE")
)
classify_facility_status(rows)$stat
}
