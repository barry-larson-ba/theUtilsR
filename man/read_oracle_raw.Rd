% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oracle_utils.R
\name{read_oracle_raw}
\alias{read_oracle_raw}
\title{Run a query on Oracle and return the result exactly as the driver types it}
\usage{
read_oracle_raw(query, con = the_connection())
}
\arguments{
\item{query}{An Oracle SELECT statement, without a trailing \verb{;}.}

\item{con}{A connection. Defaults to the configured one.}
}
\value{
A lazy table.
}
\description{
Dispatches on the connection:
}
\details{
\describe{
\item{\strong{sparklyr}}{the query is wrapped as \verb{(query) tmp} and passed
through the JDBC \code{dbtable} option, so any valid SELECT works without
naming a table and the whole statement executes on the Oracle side.}
\item{\strong{DBI}}{the query becomes a lazy \code{tbl()}, which dbplyr composes
further verbs onto as a subquery.}
}

Either way the statement becomes a subquery, so it must not end in a
semicolon, and row limits use Oracle syntax (\verb{FETCH FIRST n ROWS ONLY} or
\code{ROWNUM}) rather than \code{LIMIT}.

No type normalisation is applied; see \code{\link[=convert_integer_decimals]{convert_integer_decimals()}}.
}
