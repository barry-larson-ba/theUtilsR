% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{read_source}
\alias{read_source}
\title{Read a logical table through the active resolver}
\usage{
read_source(name, columns = NULL, where = NULL, return_type = "source")
}
\arguments{
\item{name}{A logical name from \link{TABLE_NAMES}.}

\item{columns}{Character vector of columns to project.}

\item{where}{A predicate, without the \code{WHERE} keyword.}

\item{return_type}{One of \link{RETURN_TYPES}.}
}
\value{
A table in the requested form.
}
\description{
The entry point for pipeline code. See \code{\link[=oracle_reader]{oracle_reader()}} for the argument
meanings; they are the resolver contract, not this function's own.
}
\details{
Named \code{read_source()} rather than \code{read()}, which the Python package can
afford because its modules are namespaces and R's are not.
}
\section{Validation comes first}{


\code{name} is checked here as well as in the resolver, so a typo fails before
any connection is opened.
}

