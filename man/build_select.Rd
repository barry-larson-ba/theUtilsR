% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{build_select}
\alias{build_select}
\title{Render a SELECT for one table}
\usage{
build_select(table, columns = NULL, where = NULL)
}
\arguments{
\item{table}{The physical table name.}

\item{columns}{Character vector of columns to project. Omit for \code{*}.}

\item{where}{A predicate, without the \code{WHERE} keyword.}
}
\value{
The statement, as a length-one character vector.
}
\description{
Kept separate from the readers so it can be tested, and read, without a
connection. There is deliberately no trailing semicolon: the Spark path
wraps the statement as a subquery, which a semicolon would break.
}
\examples{
build_select("rpt.msow_base_data_hist", c("pract_id"), "month = 0")
}
