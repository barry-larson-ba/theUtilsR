% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{catalog_reader}
\alias{catalog_reader}
\title{Build a resolver that reads from catalog tables instead of Oracle}
\usage{
catalog_reader(con, tables = CATALOG_TABLES)
}
\arguments{
\item{con}{A \code{spark_connection} or \code{DBIConnection} to read tables through.}

\item{tables}{Named character vector of logical -> physical. Defaults to
\link{CATALOG_TABLES}, which ships unmapped.}
}
\value{
A function suitable for \code{\link[=use_resolver]{use_resolver()}}.
}
\description{
A factory rather than a plain reader, because the catalog path needs its own
connection handle and there is no reason to put a second one into package
state -- \code{\link[=configure]{configure()}} already owns the one used for the JDBC reads.
}
\examples{
\dontrun{
use_resolver(catalog_reader(sc))
}
}
