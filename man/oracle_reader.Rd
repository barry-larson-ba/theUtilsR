% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{oracle_reader}
\alias{oracle_reader}
\title{Read a logical table from Oracle}
\usage{
oracle_reader(name, columns = NULL, where = NULL, return_type = "source")
}
\arguments{
\item{name}{A logical name from \link{TABLE_NAMES}.}

\item{columns}{Character vector of columns to project.}

\item{where}{Oracle predicate, without the \code{WHERE} keyword.}

\item{return_type}{One of \link{RETURN_TYPES}.}
}
\value{
A table in the requested form.
}
\description{
Projection and predicate are pushed into the SQL, so Oracle does the work
and only the result crosses the wire -- the same thing the SAS \verb{proc sql}
steps relied on.
}
\details{
Requires \code{\link[=configure]{configure()}} to have been called.
}
